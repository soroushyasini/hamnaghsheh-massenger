# راهنمای نصب و راه‌اندازی پلاگین چت همناقشه

## پیش‌نیازها

قبل از نصب پلاگین چت همناقشه، مطمئن شوید که موارد زیر را دارید:

1. **WordPress نسخه 5.8 یا بالاتر**
2. **PHP نسخه 7.4 یا بالاتر**
3. **پلاگین اصلی Hamnaghsheh Project Management نصب و فعال شده باشد**

## مراحل نصب

### روش 1: نصب از طریق پنل مدیریت وردپرس

1. فایل ZIP پلاگین را دانلود کنید
2. به پنل مدیریت وردپرس بروید
3. از منو به **پلاگین‌ها > افزودن** بروید
4. روی دکمه **بارگذاری پلاگین** کلیک کنید
5. فایل ZIP را انتخاب و آپلود کنید
6. روی **فعال‌سازی پلاگین** کلیک کنید

### روش 2: نصب دستی از طریق FTP

1. فایل ZIP پلاگین را از حالت فشرده خارج کنید
2. پوشه `hamnaghsheh-chat` را به `/wp-content/plugins/` آپلود کنید
3. به پنل مدیریت وردپرس بروید
4. از منو به **پلاگین‌ها** بروید
5. پلاگین **Hamnaghsheh Chat** را فعال کنید

### تایید نصب موفق

پس از فعال‌سازی، پلاگین به صورت خودکار:
- جداول دیتابیس مورد نیاز را ایجاد می‌کند
- ایندکس‌های بهینه‌ساز را اضافه می‌کند
- تنظیمات اولیه را انجام می‌دهد

برای تایید نصب موفق:
1. به یک پروژه بروید
2. جعبه چت باید در پایین سمت چپ صفحه ظاهر شود
3. روی آن کلیک کنید تا باز شود

## راهنمای استفاده

### برای کاربران عادی

#### ارسال پیام

1. به صفحه یک پروژه که عضو آن هستید بروید
2. روی جعبه چت در پایین سمت چپ کلیک کنید
3. پیام خود را در کادر ورودی تایپ کنید
4. دکمه **ارسال** را بزنید یا کلید **Enter** را فشار دهید

**نکته:** برای ایجاد خط جدید، کلیدهای **Shift + Enter** را فشار دهید.

#### منشن کردن کاربران

دو روش برای منشن کردن کاربران وجود دارد:

**روش 1: با دکمه**
1. روی دکمه **@ کاربر** کلیک کنید
2. از لیست ظاهر شده کاربر مورد نظر را انتخاب کنید
3. با کلیدهای فلش و Enter انتخاب کنید

**روش 2: تایپ مستقیم**
1. کاراکتر `@` را تایپ کنید
2. شروع به نوشتن نام کاربر کنید
3. از لیست پیشنهادی انتخاب کنید

#### منشن کردن فایل‌ها

مشابه منشن کاربران، اما با کاراکتر `#`:

1. روی دکمه **# فایل** کلیک کنید یا `#` تایپ کنید
2. شروع به نوشتن نام فایل کنید
3. فایل مورد نظر را از لیست انتخاب کنید

#### ویرایش پیام

1. روی پیام خود **دوبار کلیک** کنید
2. متن پیام را ویرایش کنید
3. دکمه **ذخیره** را بزنید

**محدودیت:** فقط تا **10 دقیقه** پس از ارسال می‌توانید پیام را ویرایش کنید.

#### مشاهده وضعیت دیده شدن

- علامت **✓** یعنی پیام ارسال شده است
- علامت **✓✓** یعنی حداقل یک نفر پیام را دیده است
- روی **✓✓** کلیک کنید تا ببینید چه کسانی پیام را دیده‌اند

#### بارگذاری پیام‌های قبلی

1. به بالای چت اسکرول کنید
2. روی دکمه **بارگذاری پیامهای قبلی** کلیک کنید
3. پیام‌های قدیمی‌تر نمایش داده می‌شوند

### برای مالکان پروژه

مالکان پروژه علاوه بر قابلیت‌های بالا، امکانات زیر را نیز دارند:

#### دانلود تاریخچه چت

1. جعبه چت را باز کنید
2. روی آیکون **📥** در هدر کلیک کنید
3. تاریخچه کامل چت به صورت فایل متنی دانلود می‌شود

**ویژگی‌های فایل خروجی:**
- فرمت UTF-8 برای نمایش صحیح فارسی
- شامل تمام پیام‌ها به ترتیب زمانی
- پیام‌های سیستمی با علامت 📄 مشخص شده‌اند
- شامل تاریخ و زمان هر پیام
- نام فایل: `chat-export-[نام-پروژه]-[تاریخ].txt`

### برای مدیران

مدیران می‌توانند تمام چت‌های پروژه‌ها را مشاهده کنند:

1. از منوی مدیریت به **همناقشه > 💬 گفتگوها** بروید
2. لیست تمام پروژه‌هایی که چت دارند نمایش داده می‌شود
3. روی دکمه **مشاهده** کلیک کنید تا به صفحه پروژه بروید

اطلاعات نمایش داده شده:
- نام پروژه
- مالک پروژه
- تعداد کل پیام‌ها
- آخرین فعالیت
- پیش‌نمایش آخرین پیام

## پیام‌های سیستمی

پلاگین به صورت خودکار برای فعالیت‌های زیر پیام سیستمی ایجاد می‌کند:

- ✅ **آپلود فایل**: "[نام کاربر] فایل [نام فایل] را آپلود کرد"
- ✅ **جایگزینی فایل**: "[نام کاربر] فایل [نام فایل] را جایگزین کرد"
- ✅ **حذف فایل**: "[نام کاربر] فایل [نام فایل] را حذف کرد"
- ✅ **دانلود فایل**: "[نام کاربر] فایل [نام فایل] را دانلود کرد"
- ✅ **مشاهده فایل**: "[نام کاربر] فایل [نام فایل] را مشاهده کرد"

**نکته:** پیام‌های دانلود و مشاهده فقط یک بار در هر 5 دقیقه برای هر کاربر و فایل ثبت می‌شوند.

## محدودیت‌ها و قوانین

### محدودیت نرخ ارسال
- حداکثر **30 پیام در دقیقه** برای هر کاربر
- در صورت تلاش برای ارسال بیش از این مقدار، پیغام خطا نمایش داده می‌شود

### محدودیت طول پیام
- حداکثر **2000 کاراکتر** برای هر پیام
- پیام‌های طولانی‌تر قبول نمی‌شوند

### محدودیت ویرایش
- فقط **10 دقیقه** پس از ارسال امکان ویرایش وجود دارد
- پیام‌های سیستمی قابل ویرایش نیستند
- فقط فرستنده پیام می‌تواند آن را ویرایش کند

### دسترسی به چت
فقط افراد زیر می‌توانند به چت هر پروژه دسترسی داشته باشند:
- مالک پروژه
- اعضای تیم (افراد تخصیص داده شده به پروژه)
- مدیران وردپرس (`manage_options`)
- کاربران با قابلیت `hamnaghsheh_admin`

## عیب‌یابی

### چت ظاهر نمی‌شود

1. مطمئن شوید پلاگین اصلی Hamnaghsheh فعال است
2. مطمئن شوید به یک پروژه دسترسی دارید
3. کش مرورگر را پاک کنید
4. بررسی کنید JavaScript فعال است

### پیام‌ها ارسال نمی‌شوند

1. مطمئن شوید اینترنت متصل است
2. بررسی کنید از محدودیت 30 پیام در دقیقه عبور نکرده‌اید
3. مطمئن شوید پیام بیشتر از 2000 کاراکتر نیست
4. کنسول مرورگر را برای خطاها بررسی کنید

### پیام‌های جدید دریافت نمی‌شوند

1. صفحه را رفرش کنید
2. چت را ببندید و دوباره باز کنید
3. مطمئن شوید اینترنت قطع نشده است
4. بررسی کنید سرور وردپرس در حال اجرا است

### نمی‌توانم پیام را ویرایش کنم

1. مطمئن شوید کمتر از 10 دقیقه از ارسال گذشته است
2. مطمئن شوید شما فرستنده پیام هستید
3. پیام‌های سیستمی قابل ویرایش نیستند

## پشتیبانی

برای گزارش مشکلات یا درخواست ویژگی‌های جدید:

- **GitHub Issues**: https://github.com/soroushyasini/hamnaghsheh-massenger/issues
- **ایمیل**: از طریق پروفایل GitHub

## به‌روزرسانی پلاگین

1. پلاگین را غیرفعال کنید (داده‌ها حفظ می‌شوند)
2. فایل‌های قدیمی را حذف کنید
3. نسخه جدید را آپلود و فعال کنید
4. تنظیمات و داده‌ها به صورت خودکار بازیابی می‌شوند

**نکته:** هرگز نیازی به حذف جداول دیتابیس نیست.

## حذف کامل پلاگین

برای حذف کامل پلاگین و تمام داده‌ها:

1. پلاگین را غیرفعال کنید
2. پلاگین را حذف کنید
3. از طریق phpMyAdmin جداول زیر را حذف کنید:
   - `{prefix}_hamnaghsheh_chat_messages`
   - `{prefix}_hamnaghsheh_chat_seen`

**هشدار:** با حذف جداول، تمام تاریخچه چت‌ها برای همیشه از بین می‌رود.
